#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)
#
#INFO su porting a V4.1
#la riga sotto non serve per V4.x, si usa invece CMakefile.txt con la riga: EMBED_FILES RefreshBtn.png
#COMPONENT_EMBED_FILES := RefreshBtn.png
